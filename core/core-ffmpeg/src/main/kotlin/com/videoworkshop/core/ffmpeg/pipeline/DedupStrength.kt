package com.videoworkshop.core.ffmpeg.pipeline

/**
 * 去重强度档位，引用领域层 [com.videoworkshop.domain.model.DedupStrength]。
 *
 * 通过 typealias 在 core-ffmpeg 模块内提供一个简短的类型别名，
 * 便于 pipeline 包及 algorithms 包内的代码引用。
 */
typealias DedupStrength = com.videoworkshop.domain.model.DedupStrength
