package com.videoworkshop.core.ffmpeg.pipeline

/**
 * 去重配置，引用领域层 [com.videoworkshop.domain.model.DedupConfig]。
 *
 * 通过 typealias 在 core-ffmpeg 模块内提供一个简短的类型别名，
 * 避免在 pipeline 包内反复书写完整包名。
 */
typealias DedupConfig = com.videoworkshop.domain.model.DedupConfig
