# Consumer ProGuard rules for :domain module.
# This is a pure-Kotlin domain module with no Android-specific keep requirements.
# Add keep rules here if any domain types are consumed via reflection downstream.
